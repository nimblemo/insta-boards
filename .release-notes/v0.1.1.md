## v0.1.1 — Docs

Patch release with documentation polish.

### Changed

- **Installation** — README now leads with the one-liner install from
  PyPI (`uv tool install insta-boards`) before the from-checkout
  instructions. The PyPI install is the recommended path for end users;
  the `uvx --from .` form stays for local development.

No code changes; the wheel is identical to `0.1.0` apart from the
version metadata.
